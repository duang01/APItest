[pytest]
addopts = -vs --alluredir  ./../report    # 指定测试运行生成的测试报告存放地址

; python_files = 'abc_*.py'   # 改写执行文件的规则，如以前是test_的现在是abc_了
; python_classes = 'Login*'   # 改写执行的类名
; python_functions = 'case*'  # 改写执行的方法名